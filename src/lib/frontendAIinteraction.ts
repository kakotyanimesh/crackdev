

/**
 * first make a req to judge zero runs the code gets its response 
 * that response with gpt prompt we are going to send to gpt
 * gets its response text file
 * convert that text file to audio 
 * gets the audio 
 * play it to the user 
 * make differenent folder for it -> frontend api calls 
 */